# invoicing · defs

JSON definitions owned by the **invoicing** module — any invoicing/time-tracking-specific screens,
mappings, or primitives — live here, co-located with the code that renders them. The folder name mirrors
the module name so a def's home is unambiguous from its path alone.
