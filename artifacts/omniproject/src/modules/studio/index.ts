/**
 * STUDIO module — the AI primitive-authoring companion (describe/image → generate → test → iterate →
 * save). Self-contained slice: page + its data hooks here, exposed through this barrel. The `defs/`
 * folder holds this module's JSON definitions (see defs/README.md).
 */
export { Studio } from "./Studio";
export * from "./studio";
