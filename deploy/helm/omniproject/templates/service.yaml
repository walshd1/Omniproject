apiVersion: v1
kind: Service
metadata:
  name: {{ include "omniproject.fullname" . }}
  labels:
    {{- include "omniproject.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "omniproject.selectorLabels" . | nindent 4 }}
