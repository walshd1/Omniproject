{
  "name": "omniproject-retention-broker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Below-the-seam retention broker: SDK-backed S3/DynamoDB/BigQuery ports for OmniProject history retention. Runs as its own process so the gateway stays zero-at-rest and SDK-free. NOT part of the pnpm workspace.",
  "scripts": {
    "start": "tsx src/server.ts",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@google-cloud/bigquery": "^8.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
