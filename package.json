{
  "name": "workspace",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "test": "pnpm -r --workspace-concurrency=1 --if-present run test",
    "test:coverage": "pnpm -r --workspace-concurrency=1 --if-present run test:coverage",
    "test:regression": "bash scripts/run-regression.sh",
    "audit": "pnpm audit --audit-level high",
    "sbom": "pnpm licenses list --json > sbom-licences.json",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "e2e:build": "PORT=3000 BASE_PATH=/ pnpm --filter @workspace/omniproject run build && pnpm --filter @workspace/api-server run build",
    "e2e:run": "playwright test",
    "e2e": "pnpm run e2e:build && playwright test"
  },
  "private": true,
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "prettier": "^3.9.5",
    "typescript": "~7.0.2"
  }
}
