{ "id": "raci-matrix", "label": "RACI matrix", "panels": [ { "id": "raci", "kind": "text", "title": "RACI matrix", "span": 12, "config": { "text": "The RACI matrix renderer isn't wired yet. This screen is defined and routed through the generic builder; drop in a `raci-grid` primitive (or bind a table panel to a RACI data source) and it renders here — no other change needed." } } ] }
