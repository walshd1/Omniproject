{
  "id": "epics",
  "label": "Epics",
  "hint": "The project's epics (epic → story → subtask). A generic data-slot register: rows are read live from the `epics` mapping slot through the same generic surface every slot uses — a pure JSON screen, no bespoke code.",
  "methodologies": ["scrum", "scrumban", "safe"],
  "route": "/epics",
  "nav": { "label": "Epics" },
  "panels": [
    {
      "id": "epics-register",
      "kind": "register",
      "title": "Epics",
      "span": 12,
      "config": {
        "slot": "epics",
        "columns": [
          { "field": "id", "label": "Id" },
          { "field": "name", "label": "Name" },
          { "field": "goal", "label": "Goal" },
          { "field": "state", "label": "State" },
          { "field": "itemIds", "label": "Items" }
        ]
      }
    }
  ]
}
