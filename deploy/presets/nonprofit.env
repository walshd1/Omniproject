# Nonprofit / NGO — docker-compose.slim.yml (lean LAN stack; demo or BYO auth)
SETTINGS_PRESET=nonprofit
AI_PROVIDER=none
# PUBLIC_URL defaults to http://localhost:3000; set to your LAN address if needed.
SESSION_SECRET=             # long random string (the one required secret on slim)
