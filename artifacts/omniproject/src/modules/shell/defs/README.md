# shell · defs

JSON definitions owned by the **shell** module — the core screen-engine app pages. Any shell-specific
screens, mappings, or primitives live here, co-located with the code that renders them. Note that most
of the app's screens are JSON defs seeded from the backend (`system.json`) and resolved at runtime; this
folder is for definitions that belong specifically to the shell pages' own code.
