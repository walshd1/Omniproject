/**
 * PROOF module — deliverable review & sign-off. A self-contained feature slice: its page, overlay
 * component, data hooks, and pure geometry all live here; the app touches it only through this barrel.
 * The `defs/` folder holds this module's JSON definitions (see defs/README.md).
 */
export { Proofs } from "./Proofs";
export { AnnotationOverlay } from "./AnnotationOverlay";
export * from "./proofs";
export * from "./proof-geometry";
