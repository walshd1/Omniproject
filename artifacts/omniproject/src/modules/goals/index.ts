/**
 * GOALS module — objectives & key results (OKRs) as a managed check-in cadence. Self-contained slice:
 * page + data hooks + pure helpers here; the app touches it only through this barrel. The `defs/` folder
 * holds this module's JSON definitions (see defs/README.md).
 */
export { Goals } from "./Goals";
export * from "./goals";
